<h1>Delete Job Application</h1>
{{!-- Only show the confirmation if there's a job application to delete --}}
{{#if jobApplication}}
  <p>Are you sure you want to delete the job application for <strong>{{jobApplication.jobTitle}}</strong> at <strong>{{jobApplication.companyName}}</strong>?</p>

  <form action="/jobApplications/delete/{{jobApplication._id}}" method="POST">
    <button type="submit" class="btn btn-danger">Yes, Delete</button>
    <a href="/jobApplications" class="btn btn-secondary">Cancel</a>
  </form>

{{else}}
  <p>Job application not found.</p>
{{/if}}